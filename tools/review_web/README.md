# Local clip review

The Flask application reads generated gesture clips from a configurable directory and writes review decisions to a separate JSON file. It does not modify canonical annotations.

See [`../../docs/REVIEW.md`](../../docs/REVIEW.md) for clip generation, application startup, decision states, and CSV export.
